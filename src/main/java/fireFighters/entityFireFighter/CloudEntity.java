package fireFighters.entityFireFighter;

import generalPackage.entity.Entity;
import util.Position;

public class CloudEntity extends Entity {
    public CloudEntity(Position position) {
        super(position);
    }
}
